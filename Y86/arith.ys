.pos 0x0 code:
    jmp _start
    

.pos 0x100 code
_start:
        irmovq $3, %rax
        irmovq $4, %rdi
        addq %rdi, %rax

        irmovq $24, %rbx
        irmovq $8, %rdi
        addq %rdi, %rbx

        irmovq $1, %rcx
        irmovq $6, %rdi
        subq %rdi, %rcx
        irmovq $5, %rdi
        addq %rdiz, %rcx

        irmovq $10, %rdx
        irmovq $6, %r8
        xorq %r8, %rdx

        halt